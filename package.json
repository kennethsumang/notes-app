{
  "name": "notes-app",
  "version": "0.0.1",
  "description": "Notes App",
  "main": "index.js",
  "repository": "https://github.com/nethzsumang/notes-app.git",
  "author": "Kenneth Sumang <dev@kennethsumang.com>",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/crypto-js": "^4.2.2",
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "start-dev-backend": "yarn --cwd backend/ start:dev",
    "start-dev-frontend": "yarn --cwd frontend/ dev",
    "start-devtest-xplatform": "cd xplatform && flutter run -d Chrome",
    "install-all": "yarn --cwd backend/ && yarn --cwd frontend/",
    "dev": "concurrently \"yarn:start-dev-*\"",
    "test": "jest"
  },
  "dependencies": {
    "crypto-js": "^4.2.0"
  }
}
